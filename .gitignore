# Secrets
.env
.env.*
!.env.example
keystore/

# Foundry
contracts/out/
# `[profile.frozen]`in KENDI artifact dizini. Ayri olmasi bir tercih degil,
# dondurulmus bytecode kapisinin CALISMA KOSULU: default/ci profilinin
# isleri `out/` icindeki ayni dosyaya yazabiliyor ve hangisinin kazandigi
# cagri sirasina bagli. Bkz. contracts/foundry.toml, [profile.frozen].
contracts/out-frozen/
contracts/cache/
contracts/broadcast/*/dry-run/
contracts/.gas-snapshot
# `make test` runs `forge test --root contracts` FROM THE REPO ROOT, and forge
# persists failing invariant sequences to `<cwd>/cache/invariant/failures/...`
# -- i.e. here, not under contracts/. Iki kez olustu. Bir sonraki basarisiz
# invariant kosusu temiz bir agaci yine kirletirdi.
cache/

# Node
node_modules/
.pnpm-store/
*.tsbuildinfo

# Next.js
web/.next/
web/out/
web/.vercel/
web/next-env.d.ts

# Test / coverage
coverage/
lcov.info
test-results/
playwright-report/

# Local data
*.log
postgres-data/
# Keeper'in Launched tarama imleci ve alarm lavabosu. Ikisi de calisma zamani
# durumudur: imlec makineye ozgu bir blok numarasi tasir, lavabo ise commit
# edilmesi anlamsiz bir olay akisidir.
keeper/.cursor
# Graduation yurutucusunun calisma zamani durumu. IMLEC AYRI BIR DOSYADIR ve
# olmasi zorunludur: izleyici ve yurutucu AYRI SUREÇLERDIR ve tek bir imlec
# dosyasina iki yazici, birinin ilerlemesini digerinin geri almasi demektir.
# `.graduate-state.json` karantina KARARLARINI tasir (zincir olgusu degil,
# bu makinenin karari); `.graduate-locks/` iki yurutucunun ayni curve'e
# birlikte gaz yakmasini engelleyen tavsiye kilitleridir.
keeper/.cursor-graduate
keeper/.graduate-state.json
keeper/.graduate-locks/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# SDD workspace (scratch)
.superpowers/

# Python bytecode from scripts/integration/ harnesses
__pycache__/
*.pyc
